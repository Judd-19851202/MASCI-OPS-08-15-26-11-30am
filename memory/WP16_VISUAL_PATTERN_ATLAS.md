# WP16 Visual Pattern Atlas

Date: 2026-07-29

## Phase 1 reconciliation update
- Evidence map verification completed for the current accepted screenshot set.
- Screenshot files present: **16**
- Screenshot files mapped to enriched Screen Registry rows: **16/16**
- No new visual winners are selected in this phase; macro pattern families remain descriptive only.

## Verified screenshot evidence set
- `WP16-EVID-PUBLIC-HUB.jpeg` → `/` → Public / Shared → Atlas 2, 9
- `WP16-EVID-PUBLIC-DAILY-FORM.jpeg` → `/daily/submit` → Public / Shared → Atlas 2, 8, 9
- `WP16-EVID-ADMIN-LOGIN.jpeg` → `/admin/login` → Admin → Atlas 1, 9
- `WP16-EVID-ADMIN-HOME.jpeg` → `/admin` → Admin → Atlas 3, 4, 5, 9
- `WP16-EVID-ADMIN-GOVERNANCE.jpeg` → `/admin/governance` → Admin → Atlas 3, 6, 9
- `WP16-EVID-PM-LOGIN.jpeg` → `/pm/login` → PM → Atlas 1, 9
- `WP16-EVID-PM-HOME.jpeg` → `/pm` → PM → Atlas 3, 4, 5, 6, 9
- `WP16-EVID-SHOP-LOGIN.jpeg` → `/shop/login` → Shop → Atlas 1, 9
- `WP16-EVID-SHOP-HOME.jpeg` → `/shop` → Shop → Atlas 3, 4, 5, 6, 9
- `WP16-EVID-HR-LOGIN.jpeg` → `/hr/login` → HR → Atlas 1, 9
- `WP16-EVID-HR-HOME.jpeg` → `/hr` → HR → Atlas 3, 4, 5, 6, 9
- `WP16-EVID-SAFETY-LOGIN.jpeg` → `/safety-portal/login` → Safety → Atlas 1, 9
- `WP16-EVID-SAFETY-HOME.jpeg` → `/safety-portal` → Safety → Atlas 3, 4, 5, 6, 7, 9
- `WP16-EVID-DISPATCH-LOGIN.jpeg` → `/dispatch-portal/login` → Dispatch → Atlas 1, 9
- `WP16-EVID-DISPATCH-HOME.jpeg` → `/dispatch-portal` → Dispatch → Atlas 3, 5, 6, 7, 9
- `WP16-EVID-HR-EMPLOYEES.jpeg` → `/hr/employees` → HR → Atlas 7, 9

## Current macro pattern families retained from the accepted draft
## 1. Login shell family

- Admin, PM, HR, Safety, Dispatch, and Shop login routes share a common structural shell with portal-specific accent divergence.

## 2. Public shell family

- Public hub and daily-report authoring routes share related public chrome but differ in work-surface density.

## 3. Authenticated shell family

- Admin, PM, HR, Safety, Dispatch, and Shop retain shared top-bar DNA but diverge in side-nav and workspace structure.

## 4. Header and search pattern family

- Shared header anatomy exists, but title language and breadcrumb/page-title usage diverge by portal.

## 5. Navigation pattern family

- Left stacked navigation competes with dispatch/transportation horizontal workspace navigation.

## 6. Dashboard and KPI pattern family

- Dashboard, KPI, and queue/map priorities vary by portal.

## 7. Table / list / queue pattern family

- HR, Safety, and Dispatch expose different list/table/queue emphasis.

## 8. Form pattern family

- Daily submit and login flows show distinct form density and guidance patterns.

## 9. Color and accent family

- Navy baseline plus portal-specific accent drift remain active.

## 10. Overlay / drawer / modal family

- Overlay families are inventoried by file but not yet exhaustively exercised.

## Sub-family enumeration status
- Deferred to Phase 6 of the evidence-expansion campaign. This checkpoint does **not** yet normalize exact sub-family counts for headers, cards, forms, buttons, tables, or icon instances.
