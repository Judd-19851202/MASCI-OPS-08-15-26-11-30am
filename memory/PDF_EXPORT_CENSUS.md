# PDF / Export Census

- **PDF-emitting modules:** 64 (grep for `render_pdf` / `make_pdf` / `reportlab` / `weasyprint` / `xhtml2pdf`).
- Every PDF path documented in the Trust Spine lifecycle (Track 15.76): `notification_queued → provider_accepted → completed`.

## Classification
- **KEEP** — all 64 modules.
- **DEFER** — Executive PDF redesign (P3 backlog per PRD).

## Zero drift
No PDF path added / removed in this deployment cluster.
