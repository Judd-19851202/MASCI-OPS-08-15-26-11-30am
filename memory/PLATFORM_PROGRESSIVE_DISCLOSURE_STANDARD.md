# PLATFORM_PROGRESSIVE_DISCLOSURE_STANDARD

Status: OPEN — PRE-C10 blocking standard

## Runtime rule

Primary work first, help second, diagnostics third, recovery last.

## Must-haves

- no persistent tutorial copy that competes with the main work area.
- lifecycle coaching collapsed by default unless the user explicitly opens it.
- destructive or recovery actions must never appear before operational-health context.
- overlays and banners must not permanently cover critical information.

## Current runtime closure

- targeted PM / Admin surfaces now expose primary operational content before helper copy.

## Open runtime checks

- employee lifecycle coaching collapsed-by-default verification.
- ALL REPORTS SYNCED / persistent helper clutter sweep.
- Admin hierarchy audit: operational health → diagnostics → recovery.