# Production Contamination Candidate Report — iter437

**Generated:** 2026-02-27 (live preview→prod scan, read-only)
**Target DB:** `masci_safety` (production)
**Method:** Direct Atlas read from preview pod (same cluster, switched DB_NAME only). Zero writes.
**Companion:** `/app/scripts/scan_production_contamination.py` (re-runnable on demand).

> ⚠ **THIS IS A CANDIDATE LIST · NO RECORDS HAVE BEEN DELETED.**
> Operator must approve each tier explicitly before any cleanup runs.

---

## TL;DR · Real contamination found = **139 records across 4 collections**

| Tier | Collection | Count | What it is | Confidence |
|---|---|---:|---|---|
| 🔴 A | `notifications` | **109** | "Failed pre-op — TST-XXX / PE-xxxxxxxx" bell notifications · equipment IDs reference TEST equipment that doesn't exist in production | 🔴 **HIGH** — safe to delete |
| 🔴 A | `tasks` | **25** | "Failed pre-op — TST-XXX / PE-xxxxxxxx" task entries · these are the parent tasks the notifications link to | 🔴 **HIGH** — safe to delete |
| 🟠 B | `field_leadership_records` | **1** | "Office Jane" time-off request (Vacation 2026-06-01 → 06-05) approved by `jaymn.judd@mascigc.com` 2026-05-13 · clearly a test entry | 🟠 **HIGH** — operator-approved test record |
| 🟠 B | `time_off_public_links` | **4** | "Office Jane", "Steve Office", "Maria Mobile", "Brand Check" — created 2026-05-13 by operator during UI testing | 🟠 **HIGH** — operator-approved test records |
| 🟡 C | `idempotency_keys` | **1** | `deploy-smoke-1778897941-3` — infrastructure smoke-test artifact from 2026-05-16 | 🟡 LOW priority · cosmetic |

**Total cleanup candidates: 139 records + 1 cosmetic artifact = 140 max.**

---

## What is NOT contamination (verified · LEAVE ALONE)

| Collection | "in window" count | Why it's real |
|---|---:|---|
| `daily_reports` | 71 | Submissions by real foremen (Ivan Lopez, Christopher Gaines, Leandro, Joe spiker, etc.) on real projects with real project_numbers |
| `meetings` | 20 | Real safety meetings (e.g., "New Hire / New-to-Site Orientation" on project 25-21) |
| `incidents` | 6 | Real incident reports |
| `operations_events` | 534 | Real dispatch board activity |
| `idempotency_keys` (4 recent) | 4 | Map to the 4 most recent real daily-reports — DO NOT DELETE these or you'll break dedup |
| `audit_events` | 9934 | Audit log — append-only history, must NEVER be touched |
| `admin_audit` + `admin_audit_log` | 1978 combined | Same — append-only |
| `session_activity` | 1043 | Real login history |
| `dispatch_users` | 2 | Real dispatcher accounts |

---

## Tier A · Equipment pre-op notification contamination (HIGH confidence)

### Why this is contamination

- `notifications` has 109 entries with titles starting `"Failed pre-op — TST-*"` or `"Failed pre-op — PE-xxxxxxxx"`.
- I cross-checked every `fleet_*` and `equipment_*` collection for asset_numbers matching `TST-*` or `PE-xxxxxxxx`. **Zero matches.** The equipment IDs these notifications reference do not exist in production.
- These notifications were generated by test fixtures during platform development (likely between 2026-05-15 and 2026-05-24) and never cleaned up.
- All 109 notifications link to one of 25 parent `tasks`, which also have `linked_source_module=None` and `linked_source_record_id=None` — orphan tasks.

### Top recurring TST/PE notification titles

| Count | Title prefix |
|---:|---|
| 12 | `Failed pre-op — TST-IT24` |
| 11 | `Failed pre-op — TST-001` |
| 2 each | `Failed pre-op — PE-b71aef21`, `PE-e4f04fcd`, `PE-4516959c`, `PE-d747c165`, `PE-a01bc3fb`, `PE-8fa59bdc`, `PE-a0f0da77`, `PE-304144c7`, `PE-3de5f11d`, `PE-6415ea46`, `PE-f2ee9eed`, `PE-2cae489f`, `PE-bff01d2e`, `PE-892121c6`, `PE-008c4721`, `PE-95c6fda4`, `PE-33d64be6`, `PE-d82d2959`, `PE-65aef03e`, `PE-299b95b3`, `PE-bf1e29b1`, `PE-80f9bbe4`, `PE-f85d2c98` |

Each TST/PE equipment generated one "task.assigned" + one "preop.failed" notification → 2 notifications per task, 23 unique TST/PE IDs across 25 tasks → matches the 109 (some have 4 notifications, slight skew).

### Proposed delete scope (Tier A)

- `notifications` WHERE `title` matches regex `^Failed pre-op — (TST-|PE-[a-f0-9]{6,})` OR `title` matches `^New task: Failed pre-op — (TST-|PE-[a-f0-9]{6,})`.
- `tasks` WHERE `title` matches regex `^Failed pre-op — (TST-|PE-[a-f0-9]{6,})`.

---

## Tier B · Operator test records from 2026-05-13 (HIGH confidence)

### `field_leadership_records` — 1 record

```
id           : (UUID, surfaced to UI as time-off request)
kind         : time_off_request
employee_name: 'Office Jane'
employee_position: '' (blank — real employees always have a position)
reason       : 'Vacation'
start_date   : 2026-06-01
end_date     : 2026-06-05
hr_decision  : {'status':'approved','notes':'All set','pay_code':'VAC','decided_by':'Super Admin','decided_by_email':'jaymn.judd@mascigc.com','decided_at':'2026-05-13T21:16:54.101208+00:00'}
```

Decided 47 seconds after creation, by the same operator who created the public link, with employee_position blank. This is the "Old/junk Time Off Request data in Admin" the operator is seeing.

### `time_off_public_links` — 4 records

| id | employee_name | position | created_by | created_at |
|---|---|---|---|---|
| `e58e027b-…` | Office Jane | (blank) | jaymn.judd@mascigc.com | 2026-05-13T21:16:52 |
| `e5df6605-…` | Steve Office | Admin Assistant | jaymn.judd@mascigc.com | 2026-05-13T21:19:57 |
| `c7322166-…` | Maria Mobile | Admin Assistant | jaymn.judd@mascigc.com | 2026-05-13T22:12:17 |
| `7089401a-…` | Brand Check | Admin | jaymn.judd@mascigc.com | 2026-05-13T22:53:49 |

All four created by the operator in a single 1h36m session on 2026-05-13. All have `employee_email=''`, `note=''`, `department` either blank or "Office". Only the "Office Jane" link's `used_record_id` resolves to the field_leadership_records row above.

### Proposed delete scope (Tier B)

- `field_leadership_records` WHERE `employee_name='Office Jane'` AND `kind='time_off_request'` (exactly 1 row).
- `time_off_public_links` WHERE `id` ∈ {`e58e027b-a3c7-4f6d-ae07-291e08ec17ec`, `e5df6605-da88-4405-889a-09c21e7e9498`, `c7322166-173c-42f7-9402-fcfc711c8dc1`, `7089401a-32a8-4db8-afe6-cae6a98a1385`} (exactly 4 rows).

---

## Tier C · Cosmetic infrastructure artifact (LOW priority)

### `idempotency_keys` — 1 record

```
key       : 'deploy-smoke-1778897941-3'
actor_id  : 'public:unknown'
response  : "{'project_name': 'DEPLOY_SMOKE', 'project_number': '', 'location': 'smoke', ..."
created_at: 2026-05-16T02:19:01.598000
```

This is a leftover idempotency entry from a previous deploy smoke test. It cannot cause incorrect behaviour (the body it was protecting has long since been delivered) — it's pure noise. Cleanup is optional/cosmetic.

There are also TWO other infrastructure idempotency keys (`postfix-1778940719`, `preview-postenv-1778939207`) that are similarly safe to retire. Total Tier C = 3 records.

### Proposed delete scope (Tier C)

- `idempotency_keys` WHERE `key` ∈ {`deploy-smoke-1778897941-3`, `postfix-1778940719`, `preview-postenv-1778939207`}.

---

## What I will NOT touch (audit-trail doctrine)

- `audit_events` — append-only history. Any "marker matches" reflect access-denial entries against `/api/.../iter437` paths or similar — they are forensic value, MUST be preserved.
- `admin_audit`, `admin_audit_log` — same.
- `session_activity`, `directory_sessions` — privacy/auth log.
- Any collection not in this report.

---

## Pre-deletion sanity checks (built into the cleanup script when written)

1. **Re-verify counts immediately before delete.** If a count has changed since the report (e.g., new daily-report submissions), abort.
2. **Each delete is by exact `_id` or `id` match** (UUID), never by regex. No bulk wildcard deletes.
3. **Dry-run mode first** — prints the exact `_id`s that WOULD be deleted, exits 0 without deleting. Operator must re-invoke with `--apply` to actually delete.
4. **Atomic per-tier** — each tier (A, B, C) is a separate transaction; aborting tier B does not partially commit tier A.
5. **Post-cleanup re-scan** — automatically re-runs `scan_production_contamination.py` and asserts the candidate counts drop to zero. Any residue surfaced loudly.
6. **Full backup of the affected rows to `/app/memory/contamination_cleanup_<ts>.json`** before deletion — operator can re-insert if any row turns out to have been real.

---

## Approval matrix · ask from operator

Reply with one of the following decisions per tier. Cleanup will only run for the tiers you explicitly approve.

| Tier | Records | Approve? |
|---|---:|---|
| **A** · 109 notifications + 25 tasks (TST/PE pre-op contamination) | **134** | __[ APPROVE / SKIP ]__ |
| **B** · 1 field_leadership_record + 4 time_off_public_links (operator test data) | **5** | __[ APPROVE / SKIP ]__ |
| **C** · 3 infrastructure idempotency keys | **3** | __[ APPROVE / SKIP ]__ |

Once you reply with the tiers approved, I will:

1. Write `/app/scripts/cleanup_production_contamination.py` (dry-run by default).
2. Run it with `--apply --tier A,B,C` (whichever you approve).
3. Re-run the contamination scan to confirm the chosen rows are gone.
4. Verify production operational records (real daily-reports / meetings / incidents counts) are unchanged.
5. Add a permanent post-deploy contamination check to deployment governance (`pre_deploy_check.sh` Sigma-III stage).
