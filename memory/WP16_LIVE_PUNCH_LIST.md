# WP16 Live Punch List

Date opened: 2026-07-30

This is the authoritative Phase B issue ledger. No repair is valid unless the issue is entered here first and later closed with verification evidence.

## Status legend
- OPEN — issue verified and awaiting repair
- REPAIRED_PENDING_VERIFICATION — repair made, evidence still required
- VERIFIED_CLOSED — defect repaired and re-tested with evidence
- BLOCKED — cannot proceed until dependency / credential / data / environment issue is resolved

## Active issues

| Issue ID | Page / surface | Severity | Screenshot / evidence | Root cause | Repair | Verification evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WP16-W1-001 | `/change-password` | Medium | Playwright 2026-07-30: `RESULT::directory_change_password_chrome::FAIL` (`caution=0`, `blueprint=0`, `footer=1`); code review `frontend/src/pages/DirectoryChangePassword.jsx` | Legacy directory password-change page rendered in a plain gray shell instead of the canonical Wave 1 auth chrome. | Re-wrap the existing form in the frozen auth shell only; preserve the current endpoint calls and rotation flow. | Playwright 2026-07-30: `RESULT::directory_change_password_chrome_after_fix::PASS` (`caution=1`, `blueprint=1`, `footer=2`, `badge=1`). | VERIFIED_CLOSED |
| WP16-W1-002 | `/field-leadership/portal/change-password` | Medium | Playwright 2026-07-30: `RESULT::fl_change_password_chrome::FAIL` (`caution=0`, `blueprint=0`, `footer=1`); code review `frontend/src/pages/FieldLeadershipPortalChangePassword.jsx` | Field Leadership change-password page rendered on a legacy plain-gray shell and bypassed the established auth-page chrome. | Move the existing form into the canonical auth shell without altering route or password-change logic. | Playwright 2026-07-30: `RESULT::fl_change_password_chrome_after_fix::PASS` (`caution=1`, `blueprint=1`, `footer=2`, `title=1`). | VERIFIED_CLOSED |
| WP16-W1-003 | `/safety/forms/login` | High | Playwright 2026-07-30: `RESULT::safety_forms_remember_me_persistence::FAIL` (`token_after_load=<cleared>`); code review confirmed login-path session wipe also came from `frontend/src/components/EnforcePortalScope.jsx` | Login page cleared `masci.safetyforms.token` on revisit because the route was treated as a full identity-switch login page. | Preserve only the Safety Forms token on `/safety/forms/login` while continuing to clear unrelated portal / directory auth state. | Playwright 2026-07-30: `RESULT::safety_forms_remember_me_persistence_after_fix::PASS` (`sf=TEST-TOKEN`, `admin=<cleared>`). | VERIFIED_CLOSED |
| WP16-W1-004 | `/safety-portal/forgot-password` | High | Playwright 2026-07-30: `RESULT::safety_forgot_dev_token_leak::FAIL`; curl 2026-07-30: `POST /api/safety/forgot-password` returned `token_for_dev`; backend source `backend/routes/safety_portal/auth_users.py:256` | Preview-only reset token was rendered directly in the operator-facing forgot-password UI. | Suppress the preview token from the user-facing surface while preserving the generic success message and existing backend contract. | Playwright 2026-07-30: `RESULT::safety_forgot_ui_after_fix::PASS` (`token_visible=False`) after submitting `cert.safety@example.com`. | VERIFIED_CLOSED |
| WP16-W1-005 | `/dispatch-portal/forgot-password` | High | Playwright 2026-07-30: `RESULT::dispatch_forgot_dev_token_leak::FAIL`; curl 2026-07-30: `POST /api/dispatch/forgot-password` returned `token_for_dev`; backend source `backend/routes/dispatch_portal_auth.py:346` | Preview-only reset token was rendered directly in the operator-facing Dispatch forgot-password UI. | Suppress the preview token from the user-facing surface while preserving the generic success message and existing backend contract. | Playwright 2026-07-30: `RESULT::dispatch_forgot_ui_after_fix_confirmed::PASS` (`token_visible=False`, `back_link=1`) after submitting `cert.dispatch@example.com`. | VERIFIED_CLOSED |
| WP16-W1-006 | `/field-leadership/portal/login` and `/leadership/login` | High | Playwright 2026-07-30: `RESULT::fl_failed_login_preserves_admin_token::FAIL` (`admin_token_after=<cleared>`); code review `frontend/src/pages/FieldLeadershipPortalLogin.jsx` showed sibling tokens cleared before request success | Shared Field Leadership login component replaced sibling tokens too early, so a failed attempt could log the user out of an already-valid admin session. | Remove the pre-request token clearing; only replace portal tokens after a confirmed successful login response. | Playwright 2026-07-30: `RESULT::fl_failed_login_preserves_admin_token_leadership_after_fix::PASS` and `RESULT::fl_failed_login_preserves_admin_token_portal_after_fix::PASS` with fresh Admin + Directory session present. | VERIFIED_CLOSED |
| WP16-W1-007 | `/dev/login` | Medium | Playwright 2026-07-30 route smoke passed visually; curl 2026-07-30: `POST /api/dev/login` returned `404`; backend contract `backend/server.py:2235-2249`; hardening test `backend/tests/test_track_24_1_hardening.py:164-165` expects 404 when disabled | Developer login page was routable while its backing preview endpoint was intentionally fail-closed, so the UI did not truthfully communicate environment status. | Keep the route visible but convert a backend 404 into an explicit disabled-state message and disable further submissions in that environment. | Playwright 2026-07-30: `RESULT::dev_login_disabled_state_after_fix::PASS` (`alert=1`, `disabled=True`) after backend returned 404. | VERIFIED_CLOSED |
| WP16-W1-008 | `/admin/login` | Medium | Code review `frontend/src/pages/AdminLogin.jsx` showed stale `/admin/hub` target; verification also exposed login-path clearing that prevented existing-session auto-elevation from ever firing | Existing-session Admin login path was broken by a stale redirect target and by the global login-path wipe clearing valid Admin + Directory session state before the redirect check could run. | Redirect existing sessions to `/admin` and preserve a valid Admin + Directory session on `/admin/login` long enough for the page's documented auto-elevation behavior to execute. | Playwright 2026-07-30: `RESULT::admin_login_autoredirect_after_fix::PASS` (`path=/admin`) with fresh Admin + Directory session present. | VERIFIED_CLOSED |
| WP16-W1-009 | Wave 1 certification register control surface (`/hr/forgot`) | Medium | Code review: `AppRoutes.jsx` includes `/hr/forgot`; `frontend/src/pages/HrForgotPassword.jsx` redirects to `/hr/login`; route was missing from `WP16_CERTIFICATION_REGISTER.csv` before this inspection | Authoritative Wave 1 register omitted an active redirect-only auth route, creating a control/inventory gap. | Add `/hr/forgot` to the certification register as a redirect-only Wave 1 surface and keep its redirect behavior tracked. | Register updated with `WP16-ROUTE-499`; Playwright 2026-07-30 `RESULT::hr_forgot_redirect::PASS` verified runtime redirect to `/hr/login`; completeness reconciliation recorded in `WP16_WAVE1_COMPLETENESS_RECONCILIATION.md`. | VERIFIED_CLOSED |

## Change-control note
- Foundation changes are frozen unless root-cause analysis proves a shared defect across multiple pages.
- Every future repair must reference this punch list before code changes are made.