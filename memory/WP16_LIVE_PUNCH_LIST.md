# WP16 Live Punch List

Date opened: 2026-07-30

This is the authoritative Phase B issue ledger. No repair is valid unless the issue is entered here first and later closed with verification evidence.

## Status legend
- OPEN — issue verified and awaiting repair
- REPAIRED_PENDING_VERIFICATION — repair made, evidence still required
- VERIFIED_CLOSED — defect repaired and re-tested with evidence
- BLOCKED — cannot proceed until dependency / credential / data / environment issue is resolved

## Active issues

| Issue ID | Page / surface | Severity | Screenshot / evidence | Root cause | Repair | Verification evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WP16-W1-001 | `/change-password` | Medium | Playwright 2026-07-30: `RESULT::directory_change_password_chrome::FAIL` (`caution=0`, `blueprint=0`, `footer=1`); code review `frontend/src/pages/DirectoryChangePassword.jsx` | Legacy directory password-change page rendered in a plain gray shell instead of the canonical Wave 1 auth chrome. | Re-wrap the existing form in the frozen auth shell only; preserve the current endpoint calls and rotation flow. | Playwright 2026-07-30: `RESULT::directory_change_password_chrome_after_fix::PASS` (`caution=1`, `blueprint=1`, `footer=2`, `badge=1`). | VERIFIED_CLOSED |
| WP16-W1-002 | `/field-leadership/portal/change-password` | Medium | Playwright 2026-07-30: `RESULT::fl_change_password_chrome::FAIL` (`caution=0`, `blueprint=0`, `footer=1`); code review `frontend/src/pages/FieldLeadershipPortalChangePassword.jsx` | Field Leadership change-password page rendered on a legacy plain-gray shell and bypassed the established auth-page chrome. | Move the existing form into the canonical auth shell without altering route or password-change logic. | Playwright 2026-07-30: `RESULT::fl_change_password_chrome_after_fix::PASS` (`caution=1`, `blueprint=1`, `footer=2`, `title=1`). | VERIFIED_CLOSED |
| WP16-W1-003 | `/safety/forms/login` | High | Playwright 2026-07-30: `RESULT::safety_forms_remember_me_persistence::FAIL` (`token_after_load=<cleared>`); code review confirmed login-path session wipe also came from `frontend/src/components/EnforcePortalScope.jsx` | Login page cleared `masci.safetyforms.token` on revisit because the route was treated as a full identity-switch login page. | Preserve only the Safety Forms token on `/safety/forms/login` while continuing to clear unrelated portal / directory auth state. | Playwright 2026-07-30: `RESULT::safety_forms_remember_me_persistence_after_fix::PASS` (`sf=TEST-TOKEN`, `admin=<cleared>`). | VERIFIED_CLOSED |
| WP16-W1-004 | `/safety-portal/forgot-password` | High | Playwright 2026-07-30: `RESULT::safety_forgot_dev_token_leak::FAIL`; curl 2026-07-30: `POST /api/safety/forgot-password` returned `token_for_dev`; backend source `backend/routes/safety_portal/auth_users.py:256` | Preview-only reset token was rendered directly in the operator-facing forgot-password UI. | Suppress the preview token from the user-facing surface while preserving the generic success message and existing backend contract. | Playwright 2026-07-30: `RESULT::safety_forgot_ui_after_fix::PASS` (`token_visible=False`) after submitting `cert.safety@example.com`. | VERIFIED_CLOSED |
| WP16-W1-005 | `/dispatch-portal/forgot-password` | High | Playwright 2026-07-30: `RESULT::dispatch_forgot_dev_token_leak::FAIL`; curl 2026-07-30: `POST /api/dispatch/forgot-password` returned `token_for_dev`; backend source `backend/routes/dispatch_portal_auth.py:346` | Preview-only reset token was rendered directly in the operator-facing Dispatch forgot-password UI. | Suppress the preview token from the user-facing surface while preserving the generic success message and existing backend contract. | Playwright 2026-07-30: `RESULT::dispatch_forgot_ui_after_fix_confirmed::PASS` (`token_visible=False`, `back_link=1`) after submitting `cert.dispatch@example.com`. | VERIFIED_CLOSED |
| WP16-W1-006 | `/field-leadership/portal/login` and `/leadership/login` | High | Playwright 2026-07-30: `RESULT::fl_failed_login_preserves_admin_token::FAIL` (`admin_token_after=<cleared>`); code review `frontend/src/pages/FieldLeadershipPortalLogin.jsx` showed sibling tokens cleared before request success | Shared Field Leadership login component replaced sibling tokens too early, so a failed attempt could log the user out of an already-valid admin session. | Remove the pre-request token clearing; only replace portal tokens after a confirmed successful login response. | Playwright 2026-07-30: `RESULT::fl_failed_login_preserves_admin_token_leadership_after_fix::PASS` and `RESULT::fl_failed_login_preserves_admin_token_portal_after_fix::PASS` with fresh Admin + Directory session present. | VERIFIED_CLOSED |
| WP16-W1-007 | `/dev/login` | Medium | Playwright 2026-07-30 route smoke passed visually; curl 2026-07-30: `POST /api/dev/login` returned `404`; backend contract `backend/server.py:2235-2249`; hardening test `backend/tests/test_track_24_1_hardening.py:164-165` expects 404 when disabled | Developer login page was routable while its backing preview endpoint was intentionally fail-closed, so the UI did not truthfully communicate environment status. | Keep the route visible but convert a backend 404 into an explicit disabled-state message and disable further submissions in that environment. | Playwright 2026-07-30: `RESULT::dev_login_disabled_state_after_fix::PASS` (`alert=1`, `disabled=True`) after backend returned 404. | VERIFIED_CLOSED |
| WP16-W1-008 | `/admin/login` | Medium | Code review `frontend/src/pages/AdminLogin.jsx` showed stale `/admin/hub` target; verification also exposed login-path clearing that prevented existing-session auto-elevation from ever firing | Existing-session Admin login path was broken by a stale redirect target and by the global login-path wipe clearing valid Admin + Directory session state before the redirect check could run. | Redirect existing sessions to `/admin` and preserve a valid Admin + Directory session on `/admin/login` long enough for the page's documented auto-elevation behavior to execute. | Playwright 2026-07-30: `RESULT::admin_login_autoredirect_after_fix::PASS` (`path=/admin`) with fresh Admin + Directory session present. | VERIFIED_CLOSED |
| WP16-W1-009 | Wave 1 certification register control surface (`/hr/forgot`) | Medium | Code review: `AppRoutes.jsx` includes `/hr/forgot`; `frontend/src/pages/HrForgotPassword.jsx` redirects to `/hr/login`; route was missing from `WP16_CERTIFICATION_REGISTER.csv` before this inspection | Authoritative Wave 1 register omitted an active redirect-only auth route, creating a control/inventory gap. | Add `/hr/forgot` to the certification register as a redirect-only Wave 1 surface and keep its redirect behavior tracked. | Register updated with `WP16-ROUTE-499`; Playwright 2026-07-30 `RESULT::hr_forgot_redirect::PASS` verified runtime redirect to `/hr/login`; completeness reconciliation recorded in `WP16_WAVE1_COMPLETENESS_RECONCILIATION.md`. | VERIFIED_CLOSED |
| WP16-W2-001 | HR / Safety / Shop home OI strips (`W2-040`, `W2-056`, `W2-064`) and their parent homes (`/hr`, `/hr/hub_v2`, `/safety-portal`, `/safety-portal/hub_v2`, `/shop`, `/shop/hub_v2`) | High | Runtime 2026-07-30 before repair on `/hr`, `/safety-portal`, and `/shop`: `Admin token required to view OI signals · request access from your administrator.` Code review: `frontend/src/components/operational_intelligence/OiAttentionStrip.jsx`. | **Corrected root cause:** the shared strip calls an admin-guarded OI summary endpoint and exposed that admin authorization failure directly on non-admin portal homes instead of falling back to portal-safe unavailable copy. | Keep the shared widget, but convert non-admin portal auth failures into portal-safe unavailable messaging while preserving admin access behavior. | Playwright 2026-07-30 after repair: HR, Safety, and Shop homes no longer show the admin-token block; each now shows portal-safe unavailable copy (`HR intelligence is unavailable...`, `Safety intelligence is unavailable...`, `Shop intelligence is unavailable...`). Admin route still loads successfully. Files modified: `frontend/src/components/operational_intelligence/OiAttentionStrip.jsx`. | VERIFIED_CLOSED |
| WP16-W2-002 | PM home root `/pm` (`W2-007`) | High | Runtime 2026-07-30 before repair: PM login and direct `/pm` resolved to `/pm/command-center`. Code review: `frontend/src/pages/PmHomeRedirect.jsx`. | PM homepage redirect target had been changed to the Command Center, moving the root entry route outside the approved Wave 2 denominator. | Restore the PM root redirect to the approved Wave 2 home at `/pm/hub`; do not alter deeper PM routes. | Playwright 2026-07-30 after repair: PM login now lands on `https://backup-forensics.preview.emergentagent.com/pm/hub`; direct `/pm` also resolves to `/pm/hub`; `[data-testid="pm-hub-v2-root"]` present in both flows. Files modified: `frontend/src/pages/PmHomeRedirect.jsx`. | VERIFIED_CLOSED |
| WP16-W2-003 | Admin OS posture strip (`W2-032`) on `/admin` (`W2-001`) | Medium | Runtime 2026-07-30 before repair: counts rendered while copy still said `Loading domain probes…`. Code review: `frontend/src/pages/admin/AdminOS.jsx`. | **Corrected root cause:** the posture strip mixed unresolved loading copy with partially resolved summary values. | Hold every posture count, including total domains, at the same loading boundary as the posture copy. | Playwright 2026-07-30 after final repair: during Admin refresh, posture copy remains `Loading domain probes…` and **all** posture columns — Healthy, Attention, Critical, Awaiting Signal, and Total Domains — hold at `—` until loading resolves. Files modified: `frontend/src/pages/admin/AdminOS.jsx`. | VERIFIED_CLOSED |
| WP16-W2-006 | Admin OS posture strip load completion on `/admin` | Medium | Final closeout verification 2026-07-30 observed: 0s = `Loading domain probes…` with `—`; ~5s = resolved to live counts; 15s = stable loaded posture. No persistent stuck-loading defect reproduced. | **Corrected root cause:** prior finding captured an intentional in-flight loading window before all probe promises resolved. `AdminOS.jsx` explicitly keeps the posture strip in loading state until every probe returns. | No production repair required. Close as **Working As Designed** and correct the certification narrative. | Final browser verification 2026-07-30: posture strip resolved from loading state to real counts within the observation window and remained stable. Files modified: none. | VERIFIED_CLOSED |
| WP16-W2-004 | Legacy/public Wave 2 homes: `/pm/hub_legacy`, `/hr/hub_legacy`, `/safety-portal/hub_legacy`, `/shop/hub_legacy`, `/safety` | Medium | Runtime 2026-07-30 before repair: these surfaces rendered without `ds-portal-shell` chrome. | Legacy/public Wave 2 home routes were bypassing the canonical `PortalShell` contract entirely. | Wrap each approved legacy/public page body in `PortalShell` only; preserve existing route logic and inner page structure. | Playwright 2026-07-30 after repair: `ds-portal-shell` is present on `/pm/hub_legacy`, `/hr/hub_legacy`, `/safety-portal/hub_legacy`, `/shop/hub_legacy`, and `/safety`. Files modified: `frontend/src/pages/PmHub.jsx`, `frontend/src/pages/HrHub.jsx`, `frontend/src/pages/SafetyHub.jsx`, `frontend/src/pages/ShopHub.jsx`, `frontend/src/pages/SafetySection.jsx`. | VERIFIED_CLOSED |
| WP16-W2-007 | Shop legacy home `/shop/hub_legacy` | Medium | Final root-cause validation 2026-07-30 isolated the alert to `ShopOpsIntelPanel` (`data-testid="ois-shop-panel-error"`) on `/shop/hub_legacy`. The panel called `/api/operations/intelligence/shop`, whose backend contract is admin-strict, and surfaced the raw auth detail to Shop users. | **Corrected root cause:** the legacy Shop hub embedded an admin-strict intelligence panel and displayed its raw 401/403 error instead of suppressing the unauthorized panel for Shop users. | Smallest-safe repair: suppress auth-error rendering for `ShopOpsIntelPanel` and hide the panel when the backend rejects the actor on auth grounds. | Final browser verification 2026-07-30: `/shop/hub_legacy` no longer shows `Admin or PM login required`; `ois-shop-panel-error` count = `0`; shell and first-screen Shop content still render. Files modified: `frontend/src/components/ShopOpsIntelPanel.jsx`. | VERIFIED_CLOSED |
| WP16-W2-005 | `/admin/platform-overview` (`W2-006`) | Low | Runtime 2026-07-30 verified again during repair: `/admin/platform-overview` resolves to `/admin` and renders `admin-os-root`. | The defect was a certification/register classification gap, not a code-runtime failure. The surface is an alias redirect, not a standalone homepage experience. | Reclassify the authoritative Wave 2 row as a redirect alias linked to the canonical Admin home. | Register updated 2026-07-30: `/admin/platform-overview` reclassified from `route_screen` to `redirect_route`, with redirect verification evidence preserved. No frontend code change required. | VERIFIED_CLOSED |
| WP16-W3-001 | `WP16_CERTIFICATION_REGISTER.csv` control row for `/admin/leadership/records` | Medium | Wave 3 inspection 2026-07-30 found the denominator route present as `W3-063` but missing from the certification register. | Phase 1 inventory captured the denominator route as `REGISTER_OMISSION`, but the certification register was not backfilled before checkpoint close. | Append a permanent certification-register row for `/admin/leadership/records` without renumbering existing Wave 3 IDs. | Wave 3 repair pass 2026-07-30 confirmed `WP16-ROUTE-500` is present in `WP16_CERTIFICATION_REGISTER.csv`; control gap closed. | VERIFIED_CLOSED |
| WP16-W3-002 | `/admin/leadership/records` | High | Testing agent iteration_81 2026-07-30: hard-load PASS, page loads 23 data rows, and first-row detail navigation stays within `/admin/leadership/records/:id`. | Production defect in route-level shell wiring: `FieldLeadershipRecords.jsx` passed `portalSwitcherCurrent="leadership"` on an `/admin/*` route. `PortalSwitcher.jsx` treats any `current` value not present in `directory.user.portals` as invalid and calls `clearDirectorySession()`, which wiped the live directory token on hard-load. | Set `portalSwitcherCurrent` from the actual route context (`admin` / `pm` / undefined) so the admin route no longer invalidates the directory session. | Independent verification: `/app/test_reports/iteration_81.json` marked WP16-W3-002 `CLOSED - PRODUCTION DEFECT FIXED`; regression spot-checks for `/admin/equipment` and `/admin/email` also PASS. | VERIFIED_CLOSED |
| WP16-W3-003 | `/admin/leadership-equipment` | High | Fresh hard-load verification 2026-07-30: route renders Equipment Catalog & Manufacturers with 41 catalog items. | Prior inspection evidence was a false positive caused by manual curl that omitted the directory-bound auth context. | No production repair required. Close after corrected live verification. | Fresh hard-load 2026-07-30: `/admin/leadership-equipment` PASS. | VERIFIED_CLOSED |
| WP16-W3-004 | `/admin/equipment` | High | Fresh hard-load verification 2026-07-30: Equipment & Suppliers loads with Equipment Status Board visible (766 units, 47 out of service, 921 overdue). | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply a shared Wave 3 admin-header repair to the affected admin routes so the board loads truthfully. | Fresh hard-load 2026-07-30: `/admin/equipment` PASS with live board data. | VERIFIED_CLOSED |
| WP16-W3-005 | `/admin/jha-plans` | High | Fresh hard-load verification 2026-07-30: `/admin/jha-plans` now loads in the Admin shell with project rows visible. | Combined defect: admin JHP library needed the shared Wave 3 admin-header repair and the admin route still rendered the safety shell. | Apply the shared Wave 3 admin-header repair and wrap the admin route in the admin shell without changing the underlying library content. | Fresh hard-load 2026-07-30: `/admin/jha-plans` PASS in Admin shell with rows visible. | VERIFIED_CLOSED |
| WP16-W3-006 | `/admin/meetings` | High | Fresh hard-load verification 2026-07-30: Safety Meetings loads with 471 meetings on file. | Prior inspection evidence was a false positive caused by manual curl that omitted the directory-bound auth context. | No production repair required. Close after corrected live verification. | Fresh hard-load 2026-07-30: `/admin/meetings` PASS. | VERIFIED_CLOSED |
| WP16-W3-007 | `/admin/qaqc` | High | Fresh hard-load verification 2026-07-30: All QA / QC Inspections loads with list content visible. | Prior inspection evidence was a false positive caused by manual curl that omitted the directory-bound auth context. | No production repair required. Close after corrected live verification. | Fresh hard-load 2026-07-30: `/admin/qaqc` PASS. | VERIFIED_CLOSED |
| WP16-W3-008 | `/admin/legacy-imports` | Medium | Fresh hard-load verification 2026-07-30: Historical Records queue loads with metadata visible (`portal: admin`, `role: admin`, `phase: B`). | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply the shared Wave 3 admin-header repair to the affected admin routes. | Fresh hard-load 2026-07-30: `/admin/legacy-imports` PASS. | VERIFIED_CLOSED |
| WP16-W3-009 | `/admin/project-staffing` | High | Fresh hard-load verification 2026-07-30: Project Staffing totals render (32 projects, 65 active assignments, 488 unassigned role slots, 3 overloaded crew). | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply the shared Wave 3 admin-header repair to the affected admin routes. | Fresh hard-load 2026-07-30: `/admin/project-staffing` PASS. | VERIFIED_CLOSED |
| WP16-W3-010 | `/admin/ai-configuration` | Medium | Fresh hard-load verification 2026-07-30: AI Configuration renders System Status and AI Health panels. | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply the shared Wave 3 admin-header repair to the affected admin routes. | Fresh hard-load 2026-07-30: `/admin/ai-configuration` PASS. | VERIFIED_CLOSED |
| WP16-W3-011 | `/admin/email` | Medium | Testing agent iteration_80 and self-test 2026-07-30: Email & Routing route hard-loads successfully without auth bounce. | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply the shared Wave 3 admin-header repair to the affected admin routes. | Testing agent iteration_80: `/admin/email` PASS. | VERIFIED_CLOSED |
| WP16-W3-012 | `/admin/jha-acknowledgements` | Medium | Fresh hard-load verification 2026-07-30: JHP Acknowledgements renders 2 projects, 6 files, 1 ack with project rows visible. | Shared Wave 3 admin routes were using non-namespaced APIs that needed explicit admin + directory headers under hard-load verification. | Apply the shared Wave 3 admin-header repair to the affected admin routes. | Fresh hard-load 2026-07-30: `/admin/jha-acknowledgements` PASS. | VERIFIED_CLOSED |
| WP16-W3-013 | `/admin/trench-safety-assets` | Medium | Fresh hard-load verification 2026-07-30: alias now resolves to `/admin/trench-safety/assets` and loads 21 trench assets in the Admin shell. | Redirect alias target pointed into the Safety portal shell instead of the admin route family. | Point the alias to `/admin/trench-safety/assets`. | Fresh hard-load 2026-07-30: `/admin/trench-safety-assets` PASS. | VERIFIED_CLOSED |

## Change-control note
- Foundation changes are frozen unless root-cause analysis proves a shared defect across multiple pages.
- Every future repair must reference this punch list before code changes are made.
