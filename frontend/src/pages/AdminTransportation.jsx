/**
 * TRACK 16.06 · `/admin/transportation` entry point.
 *
 * The old single-page Phase-1 admin UI was replaced by the Transportation
 * Experience Layer router. This file remains as the public symbol that
 * App.js imports so the older import path keeps working.
 */
export { default } from "./transportation/TransportationApp";
